<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.milu.excon2">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- android:roundIcon="@mipmap/ic_launcher_round" -->
    <application
        android:allowBackup="true"
        android:icon="@drawable/dado3d"
        android:label="@string/app_name"
        android:roundIcon="@drawable/dado3d"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RecycleActivity" />
        <activity
            android:name=".RateActivity"
            android:theme="@style/CustomTheme" />
        <activity android:name=".AnimeActivity" />
        <activity android:name=".SeekActivity" />
        <activity android:name=".ContextMenuActivity" />
        <activity android:name=".TemperatureActivity" />
        <activity android:name=".StopWatchActivity" />
        <activity
            android:name=".CustomActionBarActivity"
            android:theme="@style/CustomTheme" />
        <activity android:name=".DiceRollerActivity" />
        <activity android:name=".LargeBmpActivity" />
        <activity android:name=".TrafficLightActivity" />
        <activity android:name=".FibonnaciActivity"></activity>
    </application>

</manifest>